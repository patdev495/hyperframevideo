# Codex-assisted script drafting first

The MVP will let Codex draft the initial source-grounded script from local production-run artifacts instead of building app-owned LLM integration immediately. The production-run files must still be structured enough for a later in-app LLM integration to consume the same source evidence and selected story inputs without redesigning the pipeline.
