# CLI agent-first MVP

The MVP will be a CLI and agent-driven workflow instead of a web application. This lets the project prove the core News-to-Video Pipeline first: discover or read news, draft and approve a script, generate voiceover, build HyperFrames compositions, and render video; a web UI can be added later once those artifacts and transitions are stable.
